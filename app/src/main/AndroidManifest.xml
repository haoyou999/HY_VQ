<?xml version="1.0" encoding="utf-8"?>

<manifest 
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 软件更新：下载 APK 后拉起系统安装器 -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 局域网 UDP 组播发现：接收组播需多播权限（normal 级别，安装自动授予，无运行时弹窗） -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 房间联机（局域网 P2P 直连）：仅需普通网络权限，无 VPN/前台服务/通知依赖 -->
    <!-- HyVqVpnService / HyVqTerracottaBridge / HyVqLanBridge 已删除（废弃死代码，无活跃调用方），
         FOREGROUND_SERVICE 权限一并移除，按权限最小化原则不声明多余权限 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 文件管理模块：全盘浏览/编辑需要"所有文件访问"（用户设置里手动授权，
         未授权时模块内引导跳转 Settings.ACTION_MANAGE_APP_ALL_FILES_ACCESS_PERMISSION） -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <!-- 文件管理模块：无 root 虚拟枚举 /data/app 包目录（PackageManager.getInstalledPackages 还原目录树）。
         normal 级权限，安装自动授予；仅侧载分发，无 Play 政策问题 -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <application 
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher" 
        android:roundIcon="@mipmap/ic_launcher" 
        android:label="@string/app_name" 
        android:supportsRtl="true" 
        android:theme="@style/AppTheme">
        <activity 
            android:name="LoginActivity" 
            android:exported="true">
            <intent-filter>
                <action 
                    android:name="android.intent.action.MAIN" />
                <category 
                    android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity 
            android:name="MainActivity" 
            android:exported="false" />
        <!-- 文件管理模块：打开任意路径文件（ACTION_VIEW）经 FileProvider 授权 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <!-- 内置图片查看器：图片类文件优先由本页打开 -->
        <activity
            android:name=".ImageViewerActivity"
            android:exported="false"
            android:theme="@style/AppTheme"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout"
            android:launchMode="singleTop" />

        <!-- 内置音视频播放器：文件管理点击音视频直接调用，不走系统外部应用 -->
        <activity
            android:name=".MediaPlayerActivity"
            android:exported="false"
            android:theme="@style/AppTheme"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout"
            android:launchMode="singleTop" />

    </application>
</manifest>